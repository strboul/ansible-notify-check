- name: Test module
  hosts: localhost
  collections:
    - strboul.ansible_notify_check
  tasks:
    - name: Run module
      strboul.notify.check:
        id: test
        condition: "[[ 1 == 1 ]]"
        message: |
          Condition is truthy.

          Lang is $LANG.
          Now is $(date).
        timer:
          OnCalendar: "*-*-* *:*:00"
